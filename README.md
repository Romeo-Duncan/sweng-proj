# sweng-proj

To start the client navigate to the root directory of the project:
1) type cd Client
2) type npm start

To start the client navigate to the root directory of the project:
1)type cd server
2)type nodemon server
